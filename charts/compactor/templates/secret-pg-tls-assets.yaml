{{- if .Values.cloud.postgres.tls.ca }}
apiVersion: v1
kind: Secret
metadata:
  name: '{{ .Values.compactor.name }}-pg-tls-cert'
  labels:
    {{- include "common.namedLabels" .Values.compactor | nindent 4 }}
    {{- include "common.commonLabels" . | nindent 4 }}
type: Opaque
data:
  ca.crt: {{ .Values.cloud.postgres.tls.ca | b64enc | quote }}
{{- end }}
